server {
    listen      5000;

    location / {
        proxy_pass http://0.0.0.0:5000/;
        proxy_set_header Host qcv.space
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;    
}
}
